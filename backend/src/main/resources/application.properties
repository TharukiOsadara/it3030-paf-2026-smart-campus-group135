# ============================================
# Smart Campus Operations Hub
# Application Properties (Main)
# ============================================

spring.application.name=smartcampus

# Load .env file if present
spring.config.import=optional:file:.env[.properties]

# ====================== Disable JPA (We use MongoDB) ======================
spring.autoconfigure.exclude=org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration,org.springframework.boot.hibernate.autoconfigure.HibernateJpaAutoConfiguration

# ====================== MongoDB ======================
spring.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/smartcampus}
spring.mongodb.database=smartcampus

# MongoDB Atlas URI (uncomment and use if needed, or keep in .env)
MONGODB_URI=mongodb+srv://admin:c8N6tvFBGbIVcitz@cluster0.hpo1gtl.mongodb.net/smart-campus?retryWrites=true&w=majority

# ====================== Server ======================
server.port=${SERVER_PORT:8080}

# ====================== File Upload ======================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:30MB}
file.upload-dir=${UPLOAD_DIR:uploads/}

# ====================== JWT ======================
jwt.secret=${JWT_SECRET:SmartCampusDefaultSecret2026}
jwt.expiration=${JWT_EXPIRATION:86400000}

# ====================== OAuth2 Google ======================
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=email,profile

# ====================== CORS ======================
cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:5173}

# ====================== Logging ======================
logging.level.com.smartcampus=DEBUG
logging.level.org.springframework.security=INFO
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.org.hibernate=ERROR